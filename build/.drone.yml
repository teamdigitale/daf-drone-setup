# DA COMPLETARE
# vanno aggiunti i certificati per nexus?
# verificare deploy

pipeline:
  build:
    image: plugins/docker
    repo: nexus.teamdigitale.test/ml-api
    registry: nexus.teamdigitale.test
    tags: test
    secrets: [ nexus_username, nexus_password ]    

  deploy:
    image: vallard/drone-kube
    template: kube-deploy-test.yml
    namespace: default