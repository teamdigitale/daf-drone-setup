pipeline:
  build:
    image: hseeberger/scala-sbt
    commands:
      - sbt test

  slack:
    image: plugins/slack
    webhook: https://hooks.slack.com/services/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
    channel: random
    template: >
      {{#success build.status}}
        build {{build.number}} succeeded. Good job.
      {{else}}
        build {{build.number}} failed. Fix me please.
      {{/success}}
